extern crate hex;
use bit_vec::BitVec;
use bitvec::prelude::*;
use sha2::{Digest, Sha256};
use std::collections::HashMap;

pub fb get_branches(tree: Vec<Vec<u8>>)->Vec<Vec<u8>>{

    let branch = tree[0]; // dummy just to return correct type

    return branch


}